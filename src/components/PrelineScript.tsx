'use client';

// PrelineScript is disabled — this app uses a custom quiz UI, not Preline components.
// Kept as a stub to avoid import errors in any existing references.

export default function PrelineScript() {
  return null;
}